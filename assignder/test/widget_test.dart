import 'package:flutter_test/flutter_test.dart';

import 'package:assignder/main.dart';

void main() {
  test('AssignderApp can be created', () {
    const app = AssignderApp();
    expect(app, isA<AssignderApp>());
  });
}
